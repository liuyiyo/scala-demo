

/**
 * 单例对象是一种特殊的类，有且只有一个实例。和惰性变量一样，
 * 单例对象是延迟创建的，当它第一次被使用时创建。
 *
 * 当对象定义于顶层时(即没有包含在其他类中)，单例对象只有一个实例。
 *
 * 当对象定义在一个类或方法中时，单例对象表现得和惰性变量一样。
 *
 * 在 Java 中 static 成员对应于 Scala 中的伴生对象的普通成员。
 */
object SingletonObjectTest {


}
